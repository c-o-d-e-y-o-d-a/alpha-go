import 'package:flutter/material.dart';

class SendTokenScreen extends StatelessWidget {
  final dynamic tokenData;
  const SendTokenScreen({super.key, this.tokenData});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}